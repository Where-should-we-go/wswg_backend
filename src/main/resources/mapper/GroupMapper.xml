<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.wswg.model.dao.GroupDao">

    <insert id="createGroup"
            parameterType="com.ssafy.wswg.model.dto.GroupDto"
            useGeneratedKeys="true"
            keyProperty="groupId"
            keyColumn="group_id">
        INSERT INTO groups (group_name, owner_id)
        VALUES (#{groupName}, #{ownerId})
    </insert>

    <insert id="addMember">
        INSERT INTO user_group (group_id, user_id)
        VALUES (#{groupId}, #{userId})
        ON CONFLICT (user_id, group_id) DO NOTHING
    </insert>

    <select id="readGroupsByUserId" resultType="com.ssafy.wswg.model.dto.GroupDto">
        SELECT
            g.group_id,
            g.group_name,
            g.owner_id,
            g.created_at,
            ug.joined_at,
            COUNT(ug_all.user_id)::integer AS member_count
        FROM groups g
        INNER JOIN user_group ug
            ON ug.group_id = g.group_id
           AND ug.user_id = #{userId}
        LEFT JOIN user_group ug_all
            ON ug_all.group_id = g.group_id
        GROUP BY g.group_id, g.group_name, g.owner_id, g.created_at, ug.joined_at
        ORDER BY ug.joined_at DESC, g.group_id DESC
    </select>

    <select id="readGroupById" resultType="com.ssafy.wswg.model.dto.GroupDto">
        SELECT
            g.group_id,
            g.group_name,
            g.owner_id,
            g.created_at,
            ug.joined_at,
            COUNT(ug_all.user_id)::integer AS member_count
        FROM groups g
        INNER JOIN user_group ug
            ON ug.group_id = g.group_id
           AND ug.user_id = #{userId}
        LEFT JOIN user_group ug_all
            ON ug_all.group_id = g.group_id
        WHERE g.group_id = #{groupId}
        GROUP BY g.group_id, g.group_name, g.owner_id, g.created_at, ug.joined_at
    </select>

    <select id="countGroupOwner" resultType="int">
        SELECT COUNT(*)
        FROM groups
        WHERE group_id = #{groupId}
          AND owner_id = #{userId}
    </select>

    <select id="countGroupById" resultType="int">
        SELECT COUNT(*)
        FROM groups
        WHERE group_id = #{groupId}
    </select>

    <select id="countGroupMember" resultType="int">
        SELECT COUNT(*)
        FROM user_group
        WHERE group_id = #{groupId}
          AND user_id = #{userId}
    </select>

    <select id="countUserById" resultType="int">
        SELECT COUNT(*)
        FROM users
        WHERE id = #{userId}
    </select>

    <select id="readMembers" resultType="com.ssafy.wswg.model.dto.GroupMemberDto">
        SELECT
            u.id AS user_id,
            u.name,
            u.email,
            u.profile_image_url,
            (g.owner_id = u.id) AS owner,
            ug.joined_at
        FROM user_group ug
        INNER JOIN users u
            ON u.id = ug.user_id
        INNER JOIN groups g
            ON g.group_id = ug.group_id
        WHERE ug.group_id = #{groupId}
        ORDER BY owner DESC, ug.joined_at ASC, u.id ASC
    </select>

    <select id="readFootprints" resultType="com.ssafy.wswg.model.dto.GroupFootprintDto">
        WITH completed_trip_regions AS (
            SELECT
                t.trip_id,
                a.sido_code,
                a.gugun_code,
                COUNT(*)::integer AS attraction_count
            FROM trips t
            CROSS JOIN LATERAL jsonb_array_elements(
                CASE
                    WHEN jsonb_typeof(t.data -&gt; 'items') = 'array' THEN t.data -&gt; 'items'
                    ELSE '[]'::jsonb
                END
            ) item
            INNER JOIN attractions a
                ON a.content_id = CASE
                    WHEN COALESCE(item -&gt;&gt; 'content_id', item -&gt;&gt; 'contentId') ~ '^[0-9]+$'
                        THEN COALESCE(item -&gt;&gt; 'content_id', item -&gt;&gt; 'contentId')::integer
                    ELSE NULL
                END
            WHERE t.group_id = #{groupId}
              AND t.end_date &lt; CURRENT_DATE
              AND a.sido_code IS NOT NULL
            GROUP BY t.trip_id, a.sido_code, a.gugun_code
        )
        SELECT
            ctr.sido_code,
            s.sido_name,
            ctr.gugun_code,
            g.gugun_name,
            COUNT(DISTINCT ctr.trip_id)::integer AS visit_count,
            SUM(ctr.attraction_count)::integer AS attraction_count,
            grm.media_type AS representative_media_type,
            grm.media_url AS representative_media_url,
            ST_Y(grm.geom) AS latitude,
            ST_X(grm.geom) AS longitude
        FROM completed_trip_regions ctr
        INNER JOIN sidos s
            ON s.sido_code = ctr.sido_code
        LEFT JOIN guguns g
            ON g.sido_code = ctr.sido_code
           AND g.gugun_code = ctr.gugun_code
        LEFT JOIN group_region_media grm
            ON grm.group_id = #{groupId}
           AND grm.sido_code = ctr.sido_code
           AND COALESCE(grm.gugun_code, -1) = COALESCE(ctr.gugun_code, -1)
        GROUP BY
            ctr.sido_code,
            s.sido_name,
            ctr.gugun_code,
            g.gugun_name,
            grm.media_type,
            grm.media_url,
            grm.geom
        ORDER BY visit_count DESC, attraction_count DESC, ctr.sido_code ASC, ctr.gugun_code ASC
    </select>

    <update id="updateGroupName">
        UPDATE groups
        SET group_name = #{groupName}
        WHERE group_id = #{groupId}
    </update>

    <delete id="deleteGroup">
        DELETE FROM groups
        WHERE group_id = #{groupId}
    </delete>

</mapper>
